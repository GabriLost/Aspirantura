<project name="${project.name}">
    <bannerLeft>
        <name>${project.name}</name>
    </bannerLeft>
    <body>
        <links>
            <item name="${project.name}" href="${project.url}"/>
            <item name="Sonar" href="http://10.21.25.244/dashboard/index/sbt-jira-cup-integration-plugin"/>
        </links>
        <menu ref="reports" />
    </body>
</project>
